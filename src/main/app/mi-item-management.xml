<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns="http://www.mulesoft.org/schema/mule/core" xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
	xmlns:spring="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-current.xsd
http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd">
	<sub-flow name="listItems">
        <collection-splitter enableCorrelation="ALWAYS" doc:name="Collection Splitter"/>
        <component doc:name="Get items for given Square account">
            <singleton-object class="util.square.ItemsLister">
                <property key="apiUrl" value="${api.url}"/>
                <property key="apiVersion" value="${api.version}"/>
            </singleton-object>
        </component>
        <collection-aggregator failOnTimeout="true" doc:name="Collection Aggregator"/>
        <combine-collections-transformer doc:name="Combine Collections" doc:description="This is necessary because of an underlying Mule mechanic. This operation does not flatten arrays of arrays, but it does turn Mule collections of objects - like those created after a split-collect operation - into a single Mule message. The payload remains unaffected, but it allows operations on the payload to continue."/>
    </sub-flow>
	<sub-flow name="listCategories">
        <collection-splitter enableCorrelation="ALWAYS" doc:name="Collection Splitter"/>
        <component doc:name="Get categories for given Square account">
            <singleton-object class="util.square.CategoriesLister">
                <property key="apiUrl" value="${api.url}"/>
                <property key="apiVersion" value="${api.version}"/>
            </singleton-object>
        </component>
        <collection-aggregator failOnTimeout="true" doc:name="Collection Aggregator"/>
        <combine-collections-transformer doc:name="Combine Collections" doc:description="This is necessary because of an underlying Mule mechanic. This operation does not flatten arrays of arrays, but it does turn Mule collections of objects - like those created after a split-collect operation - into a single Mule message. The payload remains unaffected, but it allows operations on the payload to continue."/>
    </sub-flow>
    <sub-flow name="listDiscounts">
        <collection-splitter enableCorrelation="ALWAYS" doc:name="Collection Splitter"/>
        <component doc:name="Get discounts for given Square account">
            <singleton-object class="util.square.DiscountsLister">
                <property key="apiUrl" value="${api.url}"/>
                <property key="apiVersion" value="${api.version}"/>
            </singleton-object>
        </component>
        <collection-aggregator failOnTimeout="true" doc:name="Collection Aggregator"/>
        <combine-collections-transformer doc:name="Combine Collections" doc:description="This is necessary because of an underlying Mule mechanic. This operation does not flatten arrays of arrays, but it does turn Mule collections of objects - like those created after a split-collect operation - into a single Mule message. The payload remains unaffected, but it allows operations on the payload to continue."/>
    </sub-flow>
    <sub-flow name="listFees">
        <collection-splitter enableCorrelation="ALWAYS" doc:name="Collection Splitter"/>
        <component doc:name="Get items for given Square account">
            <singleton-object class="util.square.FeesLister">
                <property key="apiUrl" value="${api.url}"/>
                <property key="apiVersion" value="${api.version}"/>
            </singleton-object>
        </component>
        <collection-aggregator failOnTimeout="true" doc:name="Collection Aggregator"/>
        <combine-collections-transformer doc:name="Combine Collections" doc:description="This is necessary because of an underlying Mule mechanic. This operation does not flatten arrays of arrays, but it does turn Mule collections of objects - like those created after a split-collect operation - into a single Mule message. The payload remains unaffected, but it allows operations on the payload to continue."/>
    </sub-flow>
</mule>
